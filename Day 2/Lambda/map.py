# Scenario 2: Squaring sensor readings using map() and lambda

readings = [2, 3, 4]

squared_readings = list(map(lambda x: x**2, readings))

print("Original readings:", readings)
print("Squared readings:", squared_readings)
